### 먼저 ros_gui_kiyv_garden_cefpython 를 설치한 후에 py에서 import를 해줘야한다 
## CEFBrower python3.7 버전 이하에서만 되는 듯 하다 

FloatLayout:
    MDLabel:
        text: "Title Browsers"
        size_hint: 0.3, 0.25
        pos_hint: {"center_x": 0.5,"center_y": 0.9}

    CEFBrower:
        url: "www.google.com"
        size_hint: 0.5, 0.5
        pos_hint: {"center_x": 0.25, "center_y": 0.5}

    # CEFBrower:
    #     url: "http://localhost:8080/......."
    #     size_hint: 0.5, 0.5
    #     pos_hint: {"center_x": 0.75, "center_y": 0.5}

### 여기에 ros 패키지를 받아서 web_video_server 패키지를 다운 받은 후 roslaunch로 실행을 시키고
### web_video_server는 localhost:8080 으로 웹캠 화면을 띄울 수 있다.  
### 물론 usb cam을 실행시켜주는 imageraw로 퍼블리싱 해주는 노드도 실행을 해줘야한다 (ex: usb_cam ros패키지)
### 그래서 CEFBrower의 url 속성에 넣어주면 된다 
### wiki.ros.org/web_video_server 참고
